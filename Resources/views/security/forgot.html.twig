{% extends 'JAIUserBundle::base.html.twig' %}

{% block user %}
    <div class="page-header">
        <h1>Forgot Password</h1>
    </div>
    <div>{{ info }}</div>
	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
{% endblock %}
